================================================================================
REGISTRO DE COMANDOS — Landing Zone
Orden de ejecución durante la sesión de trabajo
================================================================================

--------------------------------------------------------------------------------
FASE 0 — Preparación del Service Principal
Archivo: scripts/sp-landing-zone-cicd.ps1
--------------------------------------------------------------------------------

  # Crear el Service Principal en Azure Entra ID
  pwsh -File scripts\sp-landing-zone-cicd.ps1


--------------------------------------------------------------------------------
FASE 2 — Bootstrap de Azure para Terraform
Archivo: scripts/bootstrap.ps1
--------------------------------------------------------------------------------

  # Modo simulación (no crea nada, solo muestra qué haría)
  pwsh -File scripts\bootstrap.ps1 -WhatIf

  # Ejecución real — crea todos los recursos
  pwsh -File scripts\bootstrap.ps1

  # Segunda ejecución — verifica idempotencia (todo debe mostrar [AVISO])
  pwsh -File scripts\bootstrap.ps1


--------------------------------------------------------------------------------
FASE 2 — Control de versiones (Git)
Directorio: landing-zone/
--------------------------------------------------------------------------------

  # Agregar el script de bootstrap al área de staging
  git add .

  # Registrar el cambio en el historial local
  git commit -m "Bootstrap completo con idempotencia"

  # Intento de push directo a main (bloqueado por el ruleset de la Fase 1)
  git push origin main

  # Crear rama feature para abrir un Pull Request
  git checkout -b feat/bootstrap

  # Subir la rama feature a GitHub
  git push origin feat/bootstrap

  # (El PR se aprobó y mergeó desde la interfaz web de GitHub)


--------------------------------------------------------------------------------
FASE 3 — Paso 1: Inicializar Terraform
Directorio: infra/terraform/
Archivos: backend.tf, providers.tf
--------------------------------------------------------------------------------

  # Descargar proveedores declarados en providers.tf
  # y conectar el backend al Storage Account de Azure
  terraform init

  # Verificar que el formato de los archivos .tf es correcto
  # (-check no modifica archivos, solo reporta si hay diferencias)
  terraform fmt -check -recursive

  # Verificar que la sintaxis del código HCL es válida
  # (no se conecta a Azure, solo analiza el código)
  terraform validate


--------------------------------------------------------------------------------
FASE 3 — Paso 2: Variables y Outputs globales
Directorio: infra/terraform/
Archivos: variables.tf, outputs.tf
--------------------------------------------------------------------------------

  # Aplicar formato automático al código HCL (corrige alineación y espacios)
  # (sin -check: modifica los archivos en lugar de solo reportar diferencias)
  terraform fmt -recursive

  # Verificar que la sintaxis del código HCL es válida
  terraform validate


--------------------------------------------------------------------------------
FASE 3 — Paso 3: Root module (main.tf) y Resource Groups
Directorio: infra/terraform/
Archivos: main.tf
--------------------------------------------------------------------------------

  # PREREQUISITO (una sola vez): Asignar Storage Blob Data Contributor al usuario
  # actual sobre el Storage Account del tfstate. Sin esto, "terraform plan" cuelga
  # al adquirir el state lock (el rol Owner en la suscripción no da acceso al
  # plano de datos del Storage Account — son planos separados en Azure).
  $me = (az ad signed-in-user show --query id --output tsv)
  az role assignment create `
    --assignee-object-id $me `
    --assignee-principal-type User `
    --role "Storage Blob Data Contributor" `
    --scope "/subscriptions/1989a8c1-76c1-4555-a228-c95f86c66635/resourceGroups/rg-platform/providers/Microsoft.Storage/storageAccounts/stlztf86c66635"

  # Aplicar formato automático
  terraform fmt -recursive

  # Verificar sintaxis
  terraform validate

  # Generar el plan de ejecución y guardarlo en un archivo binario
  # -out=tfplan: guarda el plan para aplicarlo exactamente igual después
  # Resultado: 3 to add, 0 to change, 0 to destroy
  #   + rg-network-hub-lz-dev
  #   + rg-spoke-app-lz-dev
  #   + rg-shared-lz-dev
  terraform plan -out=tfplan

  # Aplicar exactamente el plan guardado (no vuelve a calcular ni pide confirmación)
  # Resultado: Apply complete! Resources: 3 added, 0 changed, 0 destroyed.
  terraform apply "tfplan"


--------------------------------------------------------------------------------
FASE 3 — Módulo network: hub-spoke (VNets, NSGs, peering, Private DNS Zones)
Directorio: infra/terraform/
Archivos: main.tf (módulo añadido), modules/network/main.tf, variables.tf, outputs.tf
--------------------------------------------------------------------------------

  # Inicializar Terraform para que detecte el nuevo módulo local
  terraform init -upgrade

  # Aplicar formato a todos los archivos recursivamente
  terraform fmt -recursive

  # Verificar sintaxis y referencias entre módulos
  terraform validate

  # Generar plan — Resultado: Plan: 18 to add, 0 to change, 0 to destroy.
  #   VNets hub y spoke, subnets, NSGs, peering bidireccional,
  #   3 Private DNS Zones (ACR, Key Vault, AKS) + 3 DNS Zone Links
  terraform plan -out=tfplan

  # Primer intento de apply falló: CIDR 10.1.1.0/22 no está alineado a frontera /22.
  # Azure requiere 10.1.0.0/22 (múltiplo de 4 en el tercer octeto para /22).
  # Corrección: modules/network/variables.tf → spoke_subnet_aks_cidr default = "10.1.0.0/22"
  # Resultado primer apply: 16 recursos creados, 2 fallaron (subnet-aks + NSG association)
  terraform apply "tfplan"

  # Nuevo plan tras corregir CIDR — Resultado: Plan: 2 to add, 0 to change, 0 to destroy.
  terraform plan -out=tfplan

  # Apply final — Resultado: Apply complete! Resources: 2 added, 0 changed, 0 destroyed.
  # Total acumulado del módulo network: 18 recursos en Azure.
  terraform apply "tfplan"

  # Commit con todos los archivos del módulo network y glosario
  cd "C:\Users\aleja\Documents\Proyectos\Path Infra\landing-zone"
  git add infra/terraform/main.tf infra/terraform/modules/network/ docs/glosario.md
  git commit -m "feat(network): módulo hub-spoke con VNets, NSGs, peering y Private DNS Zones"
  git push origin feat/fase3-network


--------------------------------------------------------------------------------
FASE 3 — Módulo monitoring: Log Analytics Workspace + Módulo policy: Azure Policies
Directorio: infra/terraform/
Archivos: main.tf (módulos añadidos), modules/monitoring/, modules/policy/
Rama: feat/fase3-monitoring-policy
--------------------------------------------------------------------------------

  # Sincronizar main y crear rama
  cd "C:\Users\aleja\Documents\Proyectos\Path Infra\landing-zone"
  git checkout main; git pull origin main
  git checkout -b feat/fase3-monitoring-policy

  # Inicializar para que detecte los nuevos módulos locales
  cd infra/terraform
  terraform init -upgrade

  # Fix detectado en validate: azurerm v4 usa "enforce" no "enforcement_mode"
  # Se corrigió con: Get-Content ... -replace 'enforcement_mode\s*=\s*false', 'enforce = false'
  terraform fmt -recursive
  terraform validate
  # → Success! The configuration is valid.

  # Primer plan: 6 to add (1 LAW + 5 policy assignments)
  terraform plan -out=tfplan

  # Primer apply: 5/6 exitosos. Error en allowed_locations: parámetro incorrecto
  # "listOfAllowedValues" debe ser "listOfAllowedLocations" para la policy builtin e56962a6
  terraform apply "tfplan"

  # Corrección y re-plan/apply del recurso faltante
  # → Plan: 1 to add, 0 to change, 0 to destroy.
  terraform plan -out=tfplan
  terraform apply "tfplan"
  # → Apply complete! Resources: 1 added, 0 changed, 0 destroyed.
  # Total rama: 6 recursos creados (law-lz-dev + 5 policy assignments)


--------------------------------------------------------------------------------
FASE 3 — Módulo keyvault: Key Vault + Private Endpoint + Módulo acr: ACR Basic
Directorio: infra/terraform/
Archivos: main.tf (módulos + unique_suffix local), modules/keyvault/, modules/acr/
Rama: feat/fase3-keyvault-acr
--------------------------------------------------------------------------------

  git checkout -b feat/fase3-keyvault-acr

  terraform fmt -recursive && terraform validate
  # Fix: enable_rbac_authorization → rbac_authorization_enabled (azurerm v4)

  terraform plan -out=tfplan
  # → Plan: 3 to add, 0 to change, 0 to destroy.

  # Primer apply: ACR creado OK. KV falló: VaultAlreadyExists (nombre global ocupado).
  terraform apply "tfplan"

  # Fix: añadir local.unique_suffix con últimos 5 chars del subscription ID (-66635)
  # KV pasa de "kv-lz-dev" a "kv-lz-dev-66635"
  # ACR pasa de "acrlzdev" a "acrlzdev66635"
  terraform plan -out=tfplan
  # → Plan: 3 to add, 0 to change, 1 to destroy.

  terraform apply "tfplan"
  # → Apply complete! Resources: 3 added, 0 changed, 1 destroyed.
  # Recursos creados: kv-lz-dev-66635 + pe-kv-lz-dev-66635 + acrlzdev66635

  # Commit y push
  cd "C:\Users\aleja\Documents\Proyectos\Path Infra\landing-zone"
  git add infra/terraform/main.tf infra/terraform/modules/keyvault/ infra/terraform/modules/acr/ docs/commands-log.txt
  git commit -m "feat(keyvault+acr): Key Vault Standard + Private Endpoint + ACR Basic con unique_suffix"
  git push origin feat/fase3-keyvault-acr


--------------------------------------------------------------------------------
FASE 3 — Módulo aks: Azure Kubernetes Service
Directorio: infra/terraform/
Archivos: main.tf (módulo aks + outputs actualizados), modules/aks/, outputs.tf
Rama: feat/fase3-aks
--------------------------------------------------------------------------------

  git checkout -b feat/fase3-aks

  terraform init -upgrade
  terraform fmt -recursive && terraform validate

  # Fix: enable_auto_scaling → auto_scaling_enabled (azurerm v4)
  # Fix: Standard_B2s no permitido en suscripción Free Trial en eastus2
  #      → Se usa Standard_D2s_v3 (2 vCPU, 8 GB, ~$70/mes)

  terraform plan -out=tfplan
  # → Plan: 2 to add, 0 to change, 0 to destroy.
  # Recursos: azurerm_kubernetes_cluster.main + azurerm_role_assignment.acr_pull

  terraform apply "tfplan"
  # → Apply complete! Resources: 2 added, 0 changed, 0 destroyed.
  # Cluster: aks-lz-dev (Free tier, 1 nodo D2s_v3, SystemAssigned identity)
  # Role: AcrPull asignado a kubelet identity sobre acrlzdev66635

  # ⚠️ PARAR el cluster inmediatamente para evitar cargos del nodo (~$70/mes)
  az aks stop --name aks-lz-dev --resource-group rg-spoke-app-lz-dev

  # Obtener credenciales de kubectl (cuando necesites conectarte)
  az aks get-credentials --name aks-lz-dev --resource-group rg-spoke-app-lz-dev

  # Iniciar el cluster cuando lo necesites
  az aks start --name aks-lz-dev --resource-group rg-spoke-app-lz-dev

  # Commit y push
  cd "C:\Users\aleja\Documents\Proyectos\Path Infra\landing-zone"
  git add infra/terraform/main.tf infra/terraform/outputs.tf infra/terraform/modules/aks/ docs/commands-log.txt
  git commit -m "feat(aks): cluster AKS Free tier + AcrPull para kubelet identity"
  git push origin feat/fase3-aks


--------------------------------------------------------------------------------
FASE 5/6 — Seguridad y observabilidad (actualización posterior)
Directorio: infra/terraform/
Archivos: main.tf, modules/monitoring/main.tf, modules/aks/main.tf, modules/network/main.tf, outputs.tf
--------------------------------------------------------------------------------

  # Formato y validación tras cambios (Defender, diagnostics, MI federada, alertas)
  terraform fmt -recursive
  terraform validate

  # Plan general
  terraform plan -no-color -input=false

  # Primer apply falló por AKS detenido (OperationNotAllowed)
  terraform apply -auto-approve -input=false -no-color

  # Encender AKS para permitir operaciones del provider sobre el cluster
  az aks start --name aks-lz-dev --resource-group rg-spoke-app-lz-dev --no-wait
  az aks show --name aks-lz-dev --resource-group rg-spoke-app-lz-dev --query "{power:powerState.code, prov:provisioningState}" -o json

  # Ajustes realizados por errores de apply:
  # - azurerm_federated_identity_credential: parent_id -> user_assigned_identity_id
  # - Defender: subplan explícito en VirtualMachines(P2) y KeyVaults(PerKeyVault)
  # - AKS pools: upgrade_settings para eliminar drift

  # Reintento apply
  terraform apply -auto-approve -input=false -no-color

  # Verificación final: sin cambios pendientes
  terraform plan -no-color -input=false
  # -> No changes. Your infrastructure matches the configuration.


--------------------------------------------------------------------------------
FASE 5.4.2 — Revisión de recomendaciones de Defender
--------------------------------------------------------------------------------

  # Listar recomendaciones Unhealthy
  az security assessment list --query "[?status.code=='Unhealthy'].{recommendation:displayName, resource:resourceDetails.ResourceName, resourceType:resourceDetails.ResourceType}" -o table

  # Verificar planes Defender activos por API de Microsoft.Security
  az rest --method get --uri "https://management.azure.com/subscriptions/1989a8c1-76c1-4555-a228-c95f86c66635/providers/Microsoft.Security/pricings?api-version=2024-01-01" --query "value[].{name:name, pricingTier:properties.pricingTier, subPlan:properties.subPlan}" -o table


--------------------------------------------------------------------------------
FASE 6.3.3 — Prueba de alerta KQL (pods Failed)
--------------------------------------------------------------------------------

  # Crear pod de prueba que falla deliberadamente
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl run lz-failed-test --image=busybox --restart=Never -- /bin/sh -c 'exit 1'"
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl get pod lz-failed-test -o jsonpath='{.status.phase}'"

  # Segundo pod para cubrir múltiples ventanas de evaluación
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl run lz-failed-test2 --image=busybox --restart=Never -- /bin/sh -c 'exit 1'"
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl get pod lz-failed-test2 -o jsonpath='{.status.phase}'"

  # Evidencia en Log Analytics
  az monitor log-analytics workspace show --resource-group rg-platform --workspace-name law-lz-dev --query customerId -o tsv
  az monitor log-analytics query --workspace <customerId-law-lz-dev> --analytics-query "KubePodInventory | where TimeGenerated > ago(15m) | where Name == 'lz-failed-test2' | summarize failed_count=count() by PodStatus" -o table

  # Limpieza de pods de prueba
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl delete pod lz-failed-test --ignore-not-found"
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl delete pod lz-failed-test2 --ignore-not-found"


--------------------------------------------------------------------------------
KEY VAULT — RBAC data-plane + acceso temporal en laboratorio
--------------------------------------------------------------------------------

  # Asignar rol completo de data-plane para KV al usuario actual
  $uid = az ad signed-in-user show --query id -o tsv
  az role assignment create --assignee-object-id $uid --assignee-principal-type User --role "Key Vault Administrator" --scope "/subscriptions/1989a8c1-76c1-4555-a228-c95f86c66635/resourceGroups/rg-spoke-app-lz-dev/providers/Microsoft.KeyVault/vaults/kv-lz-dev-66635"

  # Ver roles sobre el vault para el usuario actual
  az role assignment list --assignee-object-id $uid --scope "/subscriptions/1989a8c1-76c1-4555-a228-c95f86c66635/resourceGroups/rg-spoke-app-lz-dev/providers/Microsoft.KeyVault/vaults/kv-lz-dev-66635" --query "[].{role:roleDefinitionName,scope:scope}" -o table

  # Sin VPN/red privada: abrir temporalmente red pública, crear secreto y cerrar
  az keyvault update --name kv-lz-dev-66635 --public-network-access Enabled
  az keyvault secret set --vault-name kv-lz-dev-66635 --name "app-message" --value "Hola desde Key Vault"
  az keyvault secret show --vault-name kv-lz-dev-66635 --name "app-message"
  az keyvault update --name kv-lz-dev-66635 --public-network-access Disabled
  az keyvault show --name kv-lz-dev-66635 --query "{name:name, publicNetworkAccess:properties.publicNetworkAccess}" -o table


--------------------------------------------------------------------------------
FASE 8 — Aplicación Flask en AKS (build, push, apply)
Directorio: landing-zone/app/
--------------------------------------------------------------------------------

  # Pre-checks (Azure CLI debe resolver SSL/proxy correctamente)
  az acr show --name acrlzdev66635 --query "{sku:sku.name,adminEnabled:adminUserEnabled}" -o table
  az aks check-acr --name aks-lz-dev --resource-group rg-spoke-app-lz-dev --acr acrlzdev66635
  az aks show --name aks-lz-dev --resource-group rg-spoke-app-lz-dev --query powerState.code -o tsv

  # Client ID de la MI del workload (alternativa al placeholder del ServiceAccount)
  cd infra/terraform
  terraform output -raw workload_app_identity_client_id

  # Despliegue completo (Docker Desktop en ejecución)
  cd ../../app
  pwsh -File scripts/deploy-phase8.ps1

  # Solo generar manifiesto combinado con client-id conocido (sin docker)
  pwsh -File scripts/deploy-phase8.ps1 -WorkloadClientId "<guid-de-terraform-output>" -SkipPush

  # Build local rápido (opcional)
  docker build -t lz-app:local .
  docker run --rm -p 8080:8080 lz-app:local

  # Apply manual equivalente al script (cluster privado + archivo adjunto)
  az aks command invoke `
    --resource-group rg-spoke-app-lz-dev `
    --name aks-lz-dev `
    --command "kubectl apply -f all.yaml" `
    --file .generated/all.yaml

  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl rollout status deployment/lz-app --timeout=120s"
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl get pods -l app=lz-app -o wide"
  az aks command invoke --resource-group rg-spoke-app-lz-dev --name aks-lz-dev --command "kubectl logs deployment/lz-app --tail=80"


--------------------------------------------------------------------------------
Runbook — Ahorro de costos (AKS apagado / encendido)
Archivos: scripts/cost-down-off.ps1, scripts/cost-down-on.ps1
Documentación: docs/runbook-cost-shutdown.md
--------------------------------------------------------------------------------

  # Simulación (no ejecuta cambios en Azure)
  cd landing-zone
  pwsh -File scripts\cost-down-off.ps1 -WhatIf
  pwsh -File scripts\cost-down-on.ps1 -WhatIf

  # Laboratorio: abrir Key Vault (público) al inicio; el script no lo vuelve a cerrar
  pwsh -File scripts\cost-down-off.ps1 -OpenKeyVaultPublicFirst

  # Apagar: limpieza K8s Fase 8 + parar AKS (sin purgar ACR)
  pwsh -File scripts\cost-down-off.ps1

  # Apagar incluyendo borrado de repos ACR (irreversible; requiere confirmación explícita)
  pwsh -File scripts\cost-down-off.ps1 -ConfirmAcrPurge

  # Solo parar AKS (sin kubectl ni ACR)
  pwsh -File scripts\cost-down-off.ps1 -SkipK8sCleanup -SkipAcrPurge

  # Encender AKS y esperar Running + nodos Ready (opcional: mismo flag Key Vault)
  pwsh -File scripts\cost-down-on.ps1 -OpenKeyVaultPublicFirst
  pwsh -File scripts\cost-down-on.ps1

  # Tras purgar ACR: volver a desplegar la app (Fase 8)
  cd app
  pwsh -File scripts\deploy-phase8.ps1